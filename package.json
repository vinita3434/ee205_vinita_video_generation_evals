{
  "name": "video-gen-evals",
  "private": true,
  "scripts": {
    "dev:all": "python scripts/run_server.py",
    "dev:all:8000": "python -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000"
  }
}
